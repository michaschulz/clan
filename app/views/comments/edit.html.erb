<% title "Antwort anpassen" %>

<%= form_for @comment do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.label :title, "Titel" %><br />
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.label :body, "Nachricht" %><br />
    <%= f.text_area :body %>
  </p>
  <p><%= f.submit %></p>
<% end %>
